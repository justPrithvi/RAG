# Web Framework (like Express.js)
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Configuration (like @nestjs/config)
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# CORS and middleware
python-multipart==0.0.6

# HTTP client for auth validation
httpx==0.26.0

# Add these as you learn and implement:
# RAG and Vector DB (uncomment when ready)
# langchain==0.1.6
# chromadb==0.4.22

# Embeddings and LLMs (optional)
# openai==1.10.0  # Uncomment if using OpenAI instead of Ollama
# sentence-transformers==2.3.1  # Uncomment for local embeddings

# Document processing
pypdf==4.0.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9  # PostgreSQL adapter
pgvector==0.2.4  # Vector extension support

